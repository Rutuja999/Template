# 🚀 Task Collaboration Platform (Angular + Spring Boot + AWS + Docker)

A production-ready, enterprise-level task management and collaboration platform built using:

- **Angular 17** (NgRx, Material, WebSockets)
- **Spring Boot 3** (JWT, JPA, Redis, Kafka)
- **PostgreSQL + Redis**
- **AWS S3 + CloudFront**
- **Docker + GitHub Actions CI/CD**

## 📁 Project Structure

